<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyCountryApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Estilos para textos -->
        <style name="textTitle">
            <item name="android:textSize">34sp</item>
            <item name="android:textColor">?attr/colorPrimary</item>
        </style>

        <style name="textTitle.bold" parent="textTitle">
            <item name="android:textStyle">bold</item>
        </style>
        <style name="textTitle.center" parent="textTitle">
            <item name="android:textAlignment">center</item>
        </style>

        <style name="textTitle.bold.center" parent="textTitle.bold">
            <item name="android:textAlignment">center</item>
        </style>

        <style name="textSubtitle">
            <item name="android:textSize">26sp</item>
            <item name = "android:textColor">?attr/colorSecondary</item>
        </style>

        <style name="textSubtitle.center" parent="textSubtitle">
            <item name="android:textAlignment">center</item>
        </style>

        <style name="textSubtitle.bold" parent="textSubtitle">
            <item name="android:textStyle">bold</item>
        </style>

        <style name="textSubSubtitle">
            <item name="android:textSize">18sp</item>
            <item name="android:textColor">?attr/colorTertiary</item>
        </style>

        <style name="textSubSubtitle.center" parent ="textSubSubtitle">
            <item name="android:textAlignment">center</item>
        </style>

        <!-- Estilos para los ShapeImageView -->
        <Style  name="roundedShapeImage">
            <item name ="cornerSize">15%</item>
        </Style>

        <style name="leafShapeImage">
            <item name="cornerSizeTopLeft">35%</item>
            <item name="cornerSizeBottomRight">35%</item>
        </style>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowExitAnimation">@android:transition/explode</item>
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorTertiary">@color/colorTertiary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSurface">@color/colorSurfaceDark</item>
    </style>
</resources>